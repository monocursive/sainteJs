import {Meteor} from 'meteor/meteor';
import React, {Component} from 'react';
import _ from 'lodash';

export class AttendeeButton extends Component {
  render() {
    return (
      <button>J'y vais</button>
    );
  }
}
